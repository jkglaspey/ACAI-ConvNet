run_main
    ConvNet (class initialization)
    CombinedLoss (class initialization)
    CustomDataset (class initialization)
        __init__
        __len__
        __getitem__
    
    train
        model.train
        optimizer.zero_grad
        model forward pass
        criterion/loss calculation
        loss.backward
        optimizer.step
        metric_logger updates
            precision.update
            recall.update
            f1.update
            bbox_error.update
    
    test
        model.eval
        model forward pass
        criterion/loss calculation
        metric_logger updates
            precision.update
            recall.update
            f1.update
            bbox_error.update
    
    log_final_results
        model.eval
        calculate_confidence_curves
        plot_confidence_curve
        confusion_matrix
        pr_curve.update
        roc_curve.update
    
    find_and_annotate_boxes
        model.eval
        draw_boxes
            cv2.rectangle
            cv2.putText
        detect_objects
            model forward pass
            softmax calculation
    
    plot_epoch_data
        plt.figure (loss plot)
        plt.figure (accuracy plot)